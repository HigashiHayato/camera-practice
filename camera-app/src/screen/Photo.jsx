import { Article, Camera } from "../components/index";

const Photo = () => {
    return (
        <div>
          <Article
            title={'カメラアプリ検証'}
          />
          <Camera />
        </div>
      );
}

export default Photo;
